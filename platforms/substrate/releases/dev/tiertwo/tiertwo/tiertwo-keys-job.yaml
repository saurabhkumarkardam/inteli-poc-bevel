apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: tiertwo-keys-job
  namespace: tiertwo-subs
  annotations:
    fluxcd.io/automated: "false"
spec:
  interval: 1m
  releaseName: tiertwo-keys-job
  chart:
    spec:
      interval: 1m
      sourceRef:
        kind: GitRepository
        name: flux-dscp
        namespace: flux-dscp
      chart: platforms/substrate/charts/substrate-key-mgmt
  values:
    node:
      name: tiertwo
      image: ghcr.io/inteli-poc/dscp-node:v4.3.1
      pullPolicy: IfNotPresent
      command: ./dscp-node
      type: member
    metadata:
      name: tiertwo-keys-job
      namespace: tiertwo-subs
    initContainer:
      image: ghcr.io/hyperledger/alpine-utils:1.0
      pullPolicy: IfNotPresent
    vault:
      address: http://vault.sub.blockchaincloudpoc-develop.com
      role: vault-role
      authpath: dscptiertwo
      serviceaccountname: vault-auth
      certsecretprefix: secretsv2/data/tiertwo
